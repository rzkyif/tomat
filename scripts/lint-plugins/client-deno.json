{
  "comment": "Plugin-only lint pass for the Svelte client UI tree. The root deno.json excludes src/ui from the recommended-rules pipeline (those rules are too opinionated for UI code we don't author from scratch); this config re-enters that tree with ONLY the tauri-boundary plugin enabled, so the @tauri-apps import discipline is still enforced.",
  "lint": {
    "rules": {
      "tags": [],
      "exclude": []
    },
    "plugins": [
      "./tauri-boundary.ts"
    ]
  }
}
