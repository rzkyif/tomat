{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Cross-platform capability for the main window. Only permissions for plugins that are registered on every platform (core, os, opener, fs, dialog) belong here; desktop-only plugin permissions live in desktop-capability so they never load on Android, where those plugins are not registered.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "os:default",
    "opener:default",
    "opener:allow-open-path",
    "fs:default",
    "dialog:default"
  ]
}
