{
  "name": "tomat-core-ptyhost",
  "exports": {},
  "tasks": {
    "build": "cargo build --release",
    "lint": "cargo clippy -q --all-targets -- -D warnings",
    "fmt": "cargo fmt",
    "fmt:check": "cargo fmt --check",
    "test": "cargo test -q"
  }
}
