---
title: Voice Input
section: Speech
order: 1
---

import UserInputDemo from "../../../components/demos/UserInputDemo.svelte";
import SettingsPanelDemo from "../../../components/demos/SettingsPanelDemo.svelte";
import Aside from "../../../components/Aside.astro";

**Voice Input** lets you dictate a message instead of typing it. It is the same
input bar you already know, with a **Speech-to-Text** engine transcribing what you
say into the box, so dictation is just another way to fill the same message rather
than a separate mode. When it is on, the bar shows a microphone.

<UserInputDemo
  scenario="voiceReady"
  highlightLabel="Voice Input"
  highlightTarget="[data-region=voice]"
/>

<Aside type="note">
  The Speech-to-Text engine can run locally, so your audio is transcribed on your machine and never
  sent away. That makes Voice Input safe to use for the same things you would type.
</Aside>

## Turning it on

Enable Speech-to-Text under its group in Settings, then pick how dictation starts.
That last choice matters more than it sounds, because it decides how the
microphone fits into the way you work: whether you tap to talk, leave it listening,
or hold a key.

<SettingsPanelDemo group="stt" />

The three ways to start are:

- **Manual**: tap the microphone to start, and tap again to stop. Voice Input
  turns off whenever the app is hidden or closed, so it never listens behind your
  back.
- **Sticky**: tap once and it keeps listening through hides, closes, and restarts,
  for a longer hands-free stretch where you do not want to keep re-arming it.
- **Push-to-Talk**: hold a global shortcut to dictate and release to send, even
  when tomat is not the focused window. A quick tap of the same shortcut shows or
  hides the window instead.

## After you speak

A raw transcription often has stray words and false starts. tomat can clean one up
with the model before the message lands, so what you dictated reads like something
you would have typed, and it can send the cleaned message for you as soon as you
stop talking, closing the loop without a final click.

It can also lower your system volume while it listens, so audio playing on your
machine does not talk over you or get mistaken for speech. Together these make
dictation feel less like operating a recorder and more like thinking aloud.

For the agent reading its answers back to you, see
[Spoken Replies](/manual/speech/spoken-replies).
