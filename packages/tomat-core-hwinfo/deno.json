{
  "name": "tomat-core-hwinfo",
  "exports": {},
  "tasks": {
    "build": "cargo build --release",
    "lint": "cargo clippy --all-targets -- -D warnings",
    "fmt": "cargo fmt",
    "fmt:check": "cargo fmt --check",
    "test": "cargo test"
  }
}
