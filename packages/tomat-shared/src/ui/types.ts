// Presentational UI types shared by the client and the website. These describe
// how a component renders, not domain data (which lives under ../domain).

/** Which screen edge a bubble (and its stack) anchors to. */
export type Alignment = "left" | "center" | "right";
